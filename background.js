chrome.runtime.onInstalled.addListener(() => {
    console.log("Extensão instalada.");
  });
  chrome.runtime.onInstalled.addListener(() => {
    console.log("Service worker ativado. Extensão instalada.");
  });
